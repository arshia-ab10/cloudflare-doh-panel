{
  "name": "cloudflare-doh-panel",
  "version": "1.0.1",
  "description": "Cloudflare Worker DoH Proxy with Camouflage & Caching",
  "main": "src/index.js",
  "bin": {
    "cloudflare-doh-panel": "bin/cli.js"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "format": "prettier --write '**/*.js'"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
